For every technology we use, answer:

What is it?
Why did we choose it?
Why not an alternative?

For example:

Why PostgreSQL instead of only MongoDB?
Why React instead of Angular?
Why Kafka instead of direct service calls?
